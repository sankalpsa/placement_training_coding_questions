#include <stdio.h>
#include<math.h>

int main()
{
    int num;
    printf("Enter the nunmber\n");
    scanf("%d",&num);
    float sq=sqrt(num);
    printf("Square root of %d is %f\n",num,sq);
    return 0;
}
